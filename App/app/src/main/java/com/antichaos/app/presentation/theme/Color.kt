package com.antichaos.app.presentation.theme

import androidx.compose.ui.graphics.Color

// Primary brand colors — warm, human, slightly rebellious (not corporate)
val Primary = Color(0xFF6C5CE7)        // Deep purple — calm focus
val OnPrimary = Color(0xFFFFFFFF)
val OnPrimaryLight = Color(0xFF1A1A2E)
val PrimaryContainer = Color(0xFF4834D4)
val PrimaryContainerLight = Color(0xFFEDE9FE)

// Secondary — supportive actions
val Secondary = Color(0xFF00CEC9)      // Teal — growth, freshness
val OnSecondary = Color(0xFFFFFFFF)

// Tertiary — accents, highlights
val Tertiary = Color(0xFFFFA502)       // Warm orange — energy, action

// Dark theme backgrounds (default)
val BackgroundDark = Color(0xFF0F0F1A)  // Very dark blue-black
val SurfaceDark = Color(0xFF1A1A2E)     // Slightly lighter for cards
val Error = Color(0xFFFF6B6B)           // Soft red (not aggressive)
val OutlineDark = Color(0xFF3D3D5C)

// Light theme backgrounds
val BackgroundLight = Color(0xFFF8F9FE)
val SurfaceLight = Color(0xFFFFFFFF)
val OutlineLight = Color(0xFFD1D5DB)

// Status colors for tasks/habits/moods
val SuccessGreen = Color(0xFF2ECC71)
val WarningYellow = Color(0xFFF39C12)
val DangerRed = Color(0xFFE74C3C)
val NeutralGray = Color(0xFF95A5A6)

// Mood gradient colors (for visualization)
val MoodVeryLow = Color(0xFF6C5CE7)    // Purple — low energy
val MoodLow = Color(0xFFA29BFE)        // Light purple
val MoodNeutral = Color(0xFF74B9FF)    // Blue — neutral
val MoodGood = Color(0xFF55EFC4)       // Green — good
val MoodGreat = Color(0xFFFFF65D)      // Yellow — great energy
